apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "todo-chatbot.fullname" . }}-config
  labels:
    {{- include "todo-chatbot.labels" . | nindent 4 }}
data:
  frontend-env: {{ .Values.config.frontendEnv | quote }}
  backend-service-url: {{ .Values.config.backendServiceUrl | quote }}
  # Add other configuration values as needed